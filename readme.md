<!-- This repositpry needs to create file keys.js, declare a variable 'secret_key' and export it
This variable needs to be equal at the secret key from chat engine: https://chatengine.io/
You need to create an account and register a new project to generate the secret key-->